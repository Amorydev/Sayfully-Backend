# Postgres trong compose.prod.yml — copy thành .env.db, đổi mật khẩu (openssl rand -hex 24).
# DATABASE_URL trong .env phải khớp: postgres://sayfully:<POSTGRES_PASSWORD>@db:5432/sayfully
POSTGRES_USER=sayfully
POSTGRES_PASSWORD=change-me
POSTGRES_DB=sayfully
